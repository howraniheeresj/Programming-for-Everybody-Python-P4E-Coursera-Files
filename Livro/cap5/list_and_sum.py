num = 0
lst = list()

ent_list = list()

while True:
    enter = raw_input('Insira números para obter a soma (quando terminar escreva pronto:')
    if enter == 'pronto'
        break
        
    try:
        enter = int(enter)
    except:
        print 'Inválido'
        continue

for n in ent_list:
    num = num + n
    lst.append(num)
    
print lst
    